import { StyleSheet } from "react-native";

export const globalStyle = StyleSheet.create({
    container: {
        padding: 24,
        paddingBottom: 50,
    },
    header: {
        alignItems: 'center',
        marginBottom: 24,
        marginTop: 10,
    },
    headerTitle: {
        fontSize: 18,
        fontWeight: 'bold',
        color: '#374151',
        marginLeft: 10,
    },
});